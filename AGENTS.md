# 全局偏好

- 默认使用简体中文回复用户。
- 代码、命令、配置键、文件路径和专有名词保持原文，必要时附中文解释。

# Windows / PowerShell 编码约束

- 在 Windows PowerShell 5.1 中读取包含中文的源码时，使用 `Get-Content -Encoding UTF8`，不要直接使用无编码参数的 `Get-Content`。
- 写入包含中文的文本文件时，优先使用 `apply_patch`。如必须使用 PowerShell 写文件，必须显式指定 UTF-8 编码。
- 不要因为终端显示乱码就判断源码文件已乱码；先用 `git diff`、编辑器或 `Get-Content -Encoding UTF8` 验证。
- 避免使用未指定编码的 `Set-Content`、`Out-File`、`>`、`>>` 重写源码文件。

# 代码注释规范

- 新建源码文件时，文件开头必须写明该文件的用途或模块名称，例如：

```ts
// ==================== 仓位管理器 ====================
```

- `interface` 必须添加注释，说明接口用途、核心字段含义或使用场景。

- `class` 必须添加注释，说明类的职责和关键行为，例如：

```ts
/**
 * 仓位管理器
 *
 * 核心职责：
 * 1. 维护全局仓位映射
 * 2. 实现"同一交易对只能持有一个仓位"的互斥限制
 */
```

- 重要函数必须添加注释，说明函数用途、关键参数、返回值或业务约束。例如 `hasConflict()`：

```ts
/**
 * 检查交易对是否可开仓
 */
function hasConflict() {
  // ...
}
```

- 关键业务逻辑、非显而易见的实现、临时兼容逻辑、风险控制逻辑和重要分支必须添加行内注释，例如：

```ts
// 需要从仓位信息中获取策略ID（如果没有，标记为 'unknown'）
```

- 注释应解释“为什么这样做”和“这段逻辑承担什么业务含义”，避免只复述代码本身。
